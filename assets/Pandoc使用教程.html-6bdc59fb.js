import{_ as n,W as r,X as l,Y as e,Z as t,$ as o,a0 as d,D as s}from"./framework-407a3ac5.js";const i={},c=d(`<p>Pandoc 是一个强大的文档转换工具，可以将各种不同格式的文档互相转换，如将 Markdown 转换成 HTML、PDF、Word、LaTeX 等格式，同时支持各种不同的标记语言。以下是一些常用的 Pandoc 用法介绍：</p><ol><li>将 Markdown 转换成 HTML：使用以下命令可以将一个 Markdown 文件转换成 HTML 格式：</li></ol><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>pandoc input.md -o output.html
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>其中 <code>input.md</code> 是要转换的 Markdown 文件，<code>output.html</code> 是生成的 HTML 文件。你可以根据需要修改文件名和路径。</p><ol start="2"><li>将 Markdown 转换成 PDF：使用以下命令可以将一个 Markdown 文件转换成 PDF 格式：</li></ol><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>pandoc input.md -o output.pdf
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,6),p={href:"https://www.latex-project.org/get/%E3%80%82",target:"_blank",rel:"noopener noreferrer"},u=d(`<ol start="3"><li>将 Markdown 转换成 Word：使用以下命令可以将一个 Markdown 文件转换成 Word 格式：</li></ol><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>pandoc input.md -o output.docx
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>注意，这需要先安装 LibreOffice 或 Microsoft Word。如果你使用的是 Windows 系统，需要将 Word 添加到系统的环境变量中。</p><ol start="4"><li>将 LaTeX 转换成 PDF：使用以下命令可以将一个 LaTeX 文件转换成 PDF 格式：</li></ol><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>pandoc input.tex -o output.pdf
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ol start="5"><li>将 HTML 转换成 Markdown：使用以下命令可以将一个 HTML 文件转换成 Markdown 格式：</li></ol><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>pandoc -s input.html -t markdown -o output.md
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>其中 <code>-s</code> 选项表示输入文件是一个 standalone HTML 文件， <code>-t</code> 选项表示输出文件的格式是 Markdown， <code>-o</code> 选项表示输出文件的名称。</p>`,8),m={href:"https://pandoc.org/MANUAL.html%E3%80%82",target:"_blank",rel:"noopener noreferrer"};function v(x,g){const a=s("ExternalLinkIcon");return r(),l("div",null,[c,e("p",null,[t("注意，这需要先安装 LaTeX 环境。如果你的系统中没有安装 LaTeX，可以参考官方文档进行安装："),e("a",p,[t("https://www.latex-project.org/get/。"),o(a)])]),u,e("p",null,[t("这些只是 Pandoc 的一些常用用法，Pandoc 还有许多高级的功能和选项，可以参考官方文档了解更多详细信息："),e("a",m,[t("https://pandoc.org/MANUAL.html。"),o(a)])])])}const h=n(i,[["render",v],["__file","Pandoc使用教程.html.vue"]]);export{h as default};
